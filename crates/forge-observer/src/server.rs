pub struct ForgeObserverService;

impl ForgeObserverService {
    pub fn new() -> Self {
        Self
    }
}

impl Default for ForgeObserverService {
    fn default() -> Self {
        Self::new()
    }
}
